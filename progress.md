# Progress Log

## Session: 2026-08-15 (continued)

### Prior rounds (complete)
- Committed `71ef781` (pushed): step-2 layout & fix round (F34–F37) — orb→attach→dots→divider→cards, armed re-record, player disc, progress clamp. Verified lint 0 / build 0 / dev 200s / grep gates.

### Current pass: wizard steps 1–3 round (F38–F46)
- **Phase A (done):** planning files rewritten (findings F38–F46, this log, this plan); contract amended — CR-064 two-payload creation, §4.10 payloads, §5 intro + CR-076 status path (`audio_attached → transcribed`), design CR-066…CR-072 + CR-073/074 (voice-ledger rows, listening pulse, compact player, mode chips + content-changed notice, in-place editor + Revert, corrected strip, voice strip auto-submit, ±-token strip).
- **Phase B (done):** editor deps installed (`@tiptap/react`, `@tiptap/starter-kit`, `@tiptap/extension-text-style`, `@tiptap/extension-color`, `@tiptap/extension-placeholder`, `dompurify` — **no `@tiptap/extension-font-size` exists on npm**; v3 TextStyle carries font size natively, recorded in §13.5). `MuiEditor` built (§46.16 contract: value/onChange/readOnly/minHeight/id, Bold/Italic/Font size/Text color toolbar, sanitize on write+render). Spec amended same change set: OQ-007 CLOSED (`raw` plain, `latest` rich HTML — recorded at §21.2, §46.16, §53, §61.3, §69, §66.9), §13.5 manifest note, §15.5 tree, §52.3 two-payload wording, §23.4 wizard-transcription-story note.
- **Phase C (done):** transport amendments — `createReportHandler` audios gate removed (two-payload), `uploadClipHandler` stagedClipId rebind path (keeps `_id`/bytes/real durationSec; first bind `draft → audio_attached`), `transcribeReportHandler` deterministic plain-Amharic `TRANSCRIBE_POOL` (no `±` in STT — kills the `± ±` blemish) + per-clip `delay()` + `failed` list, `reTranscribeHandler` plain alternate, `correctContentHandler` real staging (`report.stagedCorrection`, voice mode canned Amharic instruction — documented mock limitation), NEW `acceptCorrectionHandler`/`revertCorrectionHandler` + routes, `revertContentHandler` pre-generation `latest → null` restore, `updateVisitsHandler` empty-visits tolerance (CR-043), `toReportDto` exposes `stagedCorrection`. Endpoint hooks: `useUpdateVisitsMutation`, `useAcceptCorrectionMutation`, `useRevertCorrectionMutation`.
- **Phase D (done):** ReportNew orchestration — create at step-1 Next (metadata+visits only; 422 `details` → `form.setError` mapper; stay on step 1), re-entry PATCH (date + visits, no second report), attach act at step-2 Next (per-take `stagedClipId` rebind loop, `attached` flags, failure → stay + toast `audio.attachFailed`), close-after-create leaves directly (no dialog), step-3 Next gated on `transcribed` via `onReadyChange`. `StepNavBar` gains `nextLabel`. New `utils/sanitizeHtml.js` (the §61.4 single configuration point); MuiEditor refactored onto it.
- **Phase E (done):** `StepTranscription`, `TranscriptionTakeRow` (28px orange number disc, pulse 12px/1.4s, compact player, pending/transcribing/transcribed/failed + aria-live announce), `StorySection` (hairline divider, content-stack passage, 300ms fade/rise reveal, §54.8 notice chip, ±-token strip CR-074). `MuiAudioPlayer` `compact` prop (CR-068).
- **Phase F (done):** `EditContentSurface` (Mode 1 — MuiEditor in place + Save + Revert to original, `storyDirty` buffer pattern), `InstructionPanel` (Mode 2), `VoiceCorrectionPanel` (Mode 3 — disc + timer strip, auto-submit on stop, "Transcribing your instruction…"), `CorrectedStrip` (proposed + reason + Accept/Revert). Constants: `WIZARD.transcription`, `WIZARD.modes`, toasts `audio.attachFailed`, `transcription.retried`, `correction.staged`.
- **Phase G (verification):** lint 0 ✓ (after `set-state-in-effect` fix — `storyDirty` derivation pattern; unused `idle` removed), build 0 ✓ (after named-import fix — v3 text-style/color export named only), `dist/` deleted. Dev server on 3001 (3000 busy with pre-existing PID 12828 — left alone): all 5 key modules transform 200 ✓; server killed after. Grep gates passed: `.id` only on local ToggleButton `item.id` UI key; `fetch(` only RTK `refetch()`; `@tiptap` only in MuiEditor; `dangerouslySetInnerHTML` only in sanitized StorySection/CorrectedStrip; `±` never resolved.
- **Phase G (headless smoke):** mock transport exercised end-to-end as the UI drives it (Node 24 + resolve hook + FileReader stand-in; `mock-smoke.mjs` in temp): login → create (no audios) → stage clip → attach → transcribe (≈250ms/clip latency, plain Amharic, no ±) → Mode-1 save → Mode-2 stage/accept → Mode-3 stage/revert → pre-generation revert → re-transcribe → empty-visits PATCH. **27/27 PASS** — found + fixed one real bug: `correctContentHandler` crashed on JSON-body dispatch (`formData?.get` on a params object) → now `formData?.get?.()`. F50.
- **Listening-Desk design pass (done):** the transcription stage reimagined (frontend-design skill, user-approved): three-act ritual — listening desk (running head + orange rail + live-waveform rows with playing spotlight/dim), story manuscript (folio marks + ink-settle stagger, colophon meta "n takes · MM:SS"), revision proof-margin (iconed mode tabs, provenance-tagged strip). `TranscriptionTakeRow` (waveform/spotlight), `StorySection` (paragraphs+folios+meta), `CorrectedStrip` (proof-margin+chip), `StepTranscription` (composition), constants (+4 keys). Lint 0 ✓, build 0 ✓, dist deleted ✓, gates ✓, dev-server 200s ✓ (user's instance on 3001; my 3002 duplicate killed). F51.
- **Owner amendments round (done):** (1) attach success toast `audio.attached` on step-2 Next; (2) lead/hint block removed + unused constants deleted (step opens on the ledger head); (3) heard-stays-heard — one Transcribe act covers only untranscribed takes (transport already skipped heard clips; status derivation already held), verified by the smoke's new re-entry block (35/35 PASS); (4) rows full width (inset dropped). Mirrors: contract CR-064/066/067 + spec §52.7 bullet. Lint 0 ✓, build 0 ✓, dist deleted ✓, dev-server 200s ✓, gates ✓. F52.
- **Re-entry dead-end fix (done):** readiness was `report?.status === "transcribed"` — BR-10 never rewinds, so a new take attached at `transcribed` left the Transcribe button hidden with no way to act. Now data-derived: `ready = clips.length > 0 && every clip has a transcription` — button reappears for the unheard take, manuscript hides until every take is heard, Next stays gated. Transport untouched. Smoke 37/37 PASS (BR-10 no-rewind + derived-ready premise asserted). Mirrors: CR-067 + §52.7. Lint 0 ✓, build 0 ✓, dist deleted ✓, dev-server 200 ✓. F53.
- **Console warnings fixed (done):** ToggleButton `startIcon` — v9.3.1 no longer consumes it (leaked to DOM, icons never rendered) → icon rendered manually in a span; MuiEditor placeholder selectors `:first-child` → `:first-of-type` (SSR warning). Dev-only, no mirrors. Lint 0 ✓, build 0 ✓, dist deleted ✓. F54.
- **Responsive transcription editor + zero-lag (done, round-5):** MuiEditor boundary contract (`onUpdate` → dirty flag only; `getContent()` ref at boundaries; seed applies unfocused-only with blur deferral; `shouldRerenderOnTransaction:false` + `useEditorState`; toolbar scrolls on overflowX; content maxHeight+scroll; responsive minHeight). EditContentSurface (status line Saved/Unsaved/No changes, stacked full-width actions at xs). MuiProviderSelect (new). Instruction/Voice panels carry the provider selector (inline ≥sm, full-width xs). Transport: provider validated/defaulted/recorded on staged. StepTranscription: single-surface revision act — editor always visible, modes swap controls below, staged candidate fills the editor read-only + Candidate·via chip, strip → action bar; mode selector 3 equal segments at xs. Mirrors: §35.2/§31.6/§11.4/§46.16/§46.17/§53.3/§53.5/§54.2/§54.5/§54.6/§54.7/§54.9 + CR-070…073/077. Smoke 46/46 PASS. Lint 0 ✓, build 0 ✓, dist deleted ✓, transforms 200 ✓, gates ✓. F55.
- **Phase H: commit on explicit user approval (pending).**

## Repository state
- Branch `phase-4-frontend-pages`; HEAD `71ef781` (pushed). Working tree has this round's source changes; planning docs + contract + spec untracked (same-change discipline at commit).- **editor.md correction pass (done, round-6):** candidate model (correct returns candidate → live editor → Save persists; staged/accept machinery deleted: accept/revert endpoints + handlers + `stagedCorrection` + CorrectedStrip + EditContentSurface). One persistent always-editable editor across modes (never remounted; `applyCandidate` imperative). Toolbar rebuilt: B I U · Paragraph/Heading · lists · align · Undo/Redo — font-size/color removed; deps: +underline +text-align, -color -text-style. §43.5 typography (Inter fallback, lh 1.75). Persistent footer in every mode with Saving…/✓ Saved just now states; Revert enabled when latest-or-dirty. Instruction field small; voice heading + labeled record button. Mirrors: spec §13.5/§14.3/§14.4/§15.5/§31.6/§35.1/§35.5/§35.7/§35.8/§46.16/§46.17/§51.3/§52.7/§53.5/§54.1-9/§68.4 + contract CR-070…073/077. Smoke 47/47 PASS. Lint 0 ✓, build 0 ✓, dist deleted ✓, transforms 200 ✓ (user's :3001), gates ✓. F56.
- **Phase H: commit on explicit user approval (pending).**
- **Voice-correction wiring fix (done):** `correctContent` query wrapped the FormData as `{ formData }` — the adapter only extracts `instanceof FormData` (uploadClip's shape), so voice submissions hit the fallback text path with no instruction → "Check the highlighted fields". Query now passes the raw FormData (`body: formData instanceof FormData ? formData : jsonBody`) — fixes mock + production. Smoke +2 checks (wrapped-shape guard + raw voice branch) — 51/51 PASS. Lint 0 ✓, build 0 ✓, dist deleted ✓, transforms 200 ✓. F57.
- **Round-7 transcription restructure (done):** 14-point owner R3 review implemented. StepTranscription = AudioCard + TranscriptionCard (Card/CardHeader/CardActions per §44 surfaces; header avatar tinted-square icon treatment; ledger head + heard-count `x of y` + scroll box xs 240/sm 280/md+ 320 + single Transcribe act; story card = circular AutoFix opener, borderless editor, persistent icon footer with color states). CorrectionDialog (fullscreen <600px, standard field, provider+mic row below right-aligned wrapping xs, Apply busy/disabled rules, closes on success / stays open on error). Mode group, StorySection, `components/reports/` deleted. Toolbar: font-size bucket select (Default/18/22/26, selection-level TextStyle) + restyled Paragraph/Heading select (`toolbarSelectSx`). MuiEditor `borderless` prop; `@tiptap/extension-text-style@^3.30.1` reinstalled. STT-only endpoint `POST /reports/:reportId/correct/transcribe` + `useTranscribeInstructionMutation` (raw FormData). `AI_PROVIDER_LABELS.nvidia = 'Deepseek'` (wire id unchanged). CR-scrub: 0 CR- refs in client/src (JSDoc cites § only; CR numbers doc-only). Smoke +6 STT checks → 56/56 PASS. Lint 0 ✓, build 0 ✓ (named TextStyle import; hook export added), dist deleted ✓, 8 modules 200 ✓, gates ✓. F58.
- **Mirrors (done):** spec §11.4/§11.5/§13.5/§14.4/§15.5/§31.6/§33.8/§35.2/§35.6/§46.16/§46.17/§52.7/§53.5/§54.1-9/§66.10 + contract CR-066/069/070/071/073/074/077 round-7 bullets (CR-078 doc-only — no code refs). Findings F58.
- **Phase H: commit on explicit user approval (pending).**

## 2026-08-17 — Round-8: 11-point dialog/editor fix pass (F59)
Implemented and verified all 11 points: CardHeader `slotProps` conversion (AudioCard/TranscriptionCard), explicit Underline removed (StarterKit v3 ships it), FONT_SIZES ladder Default/10/11/12/14/16 (Default = ~17px sentinel), `setFontSize(\`${next}px\`)` (extension renders the style bare; the TypeError was a stale dev bundle), `??`→`||` Save fallback, xs toolbar scroll rail (nowrap/overflow-auto/hidden dividers/compact select), dialog field editor-like borderless (placeholder kept), memoized dialog + stable recorder callback, **`result.text`/`result.content` reads** (normalizeResult unwraps the envelope — fixed the empty STT field AND the silently no-oping Apply), label-less provider select. Smoke 56/56, lint 0, build 0 + dist deleted, 7/7 modules transform 200, grep gates clean. Mirrors: spec §11.5/§46.16/§46.17/§54.2, contract CR-070/071/077, findings F59, task_plan round-8. Commit (round-7 Phase I + round-8) gated on approval.

## 2026-08-17 — Round-8.1: zero-lag dialog field, font-size crash-proofing, xs rail squish (F60)
User follow-up round. (1) Font-size TypeError: static pipeline re-verified end-to-end (dist + optimized dep bundle + served module all contain setFontSize; single deduped core; restart confirmed via PID change) — environmental (stale tab graph), cured by hard reload; hardened anyway with the core `setMark("textStyle", …)` fallback. (2) xs/sm toolbar squish: real bug — nowrap flex children compress (flex-shrink default) instead of scrolling; fixed with `& > * { flexShrink: 0 }`. (3) Dialog: 5a/5c/5d confirmed already implemented; 5b typing lag got the real fix — the zero-lag `InstructionField` (value owned inside, emptiness-flip callback, imperative getValue/seed); recorder callbacks + dialog onClose stabilized. Lint 0, build 0 + dist deleted, smoke 56/56, transforms 200. Mirrors: spec §46.16/§54.2, findings F60, task_plan round-8.1. Commit (round-7 Phase I + round-8 + 8.1) still gated on approval.

## 2026-08-17 — Round-8.2: font-size paragraph scope + the highlighted-field contract (F61)
User reported BOTH issues persisting after a full Chrome hard reload (stale-tab theory dead). Font-size: root cause FOUND in the installed core — `setFontSize`'s nested chain dispatches the stored mark, then the outer chain's `focus()` transaction (storedMarks captured null) wipes it; collapsed caret = no visible effect ever. Fixed with paragraph scope (setTextSelection to the enclosing paragraph in the same chain — visible apply + select reflection). Save 422: systemic — the §42.4 `fieldErrors` map was never consumed anywhere (ReportNew read the dead `error.data.details`), so "Check the highlighted fields" highlighted nothing; fixed via local pre-validation + `fieldErrors.content`/`fieldErrors.instruction` mappings onto a new MuiEditor `fieldError` prop and the InstructionField; CorrectionDialog apply contract rethrows so the dialog can highlight; ReportNew reads the normalized shape. Temporary diagnostics added (schema audit, getContent empty-read, run() result, save-block) to pin the empty-content source in the user's flow — REMOVED after the repro. Lint 0, build 0 + dist deleted, smoke 56/56, CR gate 0. Mirrors: spec §46.16/§53.5/§54.2/§52.10, findings F61, task_plan round-8.2. Commit (round-7 Phase I + round-8 + 8.1 + 8.2) still gated on approval.

## 2026-08-17 — Round-8.3: the four round-8.2 defects (F62)
All four user reports root-caused and fixed. (1) Save-empty: `StepTranscription.surfaceRef` was NEVER passed down — TranscriptionCard owned a separate ref, so every boundary read was `undefined` → eternal "Write the story before saving" (and, pre-8.2, the eternal 422). Fix: the card accepts the step's `ref` as a plain React 19 prop and forwards it; no own ref. (2) Correction replaced the story: the mock returned the INSTRUCTION's first line as the candidate. Fix: `buildCorrectionCandidate` — the full story with the deterministic §35 partial edit (verb-dedup / case-FE move, reason vocabulary, ± verbatim, unchanged-with-reason when the pattern is absent); dead `escapeHtml` removed. (3) Success toast with the generic error copy: `normalizeResult` drops the envelope `message`, so `result?.message` always fell back to `error.generic`. Fix: `TOAST_CATALOGUE.transcription.saved`/`reverted` constants as the copy source. (4) Font-size: the round-8.2 paragraph scope was structurally ineffective — the extension's `setFontSize`/`unsetFontSize` are themselves nested chains dispatching on the collapsed state before the outer selection applies, and the outer dispatch wipes the stored mark. Fix: paragraph expansion + core `setMark`/`unsetMark` in the SAME outer chain. All round-8.2 temp diagnostics removed (console gate 0). Lint 0, build 0 + dist deleted, smoke 56/56, CR gate 0. Mirrors: spec §46.16/§53.5/§54.3/§60.6/§66.10, findings F62, task_plan round-8.3. Commit (round-7 Phase I + round-8 + 8.1 + 8.2 + 8.3) still gated on approval.

## 2026-08-17 — Round-8.4: raw-dispatch font-size + text-empty boundary read (F63)
Two user reports after round-8.3. (1) Font-size "focuses on the text": the round-8.2/8.3 paragraph-expansion SELECTED the whole paragraph — the visible defect. Fix: the size now applies via a raw transaction that never changes the selection (`tr.addMark($from.start(), $from.end(), schema.mark("textStyle", {fontSize}))` / `removeMark`); empty paragraphs take the stored-marks path with a new `fontSizeIntent` selectionUpdate re-assertion (any selection transaction wipes `storedMarks` — the "always Default" snap — now re-asserted while the caret is in an empty paragraph; guarded, no loop). (2) Empty save persisted: an empty TipTap doc serializes as `<p></p>` — truthy — so it passed both `.trim()` guards and saved with the success toast. Fix on both sides: `getContent()` now returns `""` when the document has no text (text-empty boundary contract, §53.3); `handleSave` restored to `??` (the round-8 `||` would resurrect the stale draft after a full delete); the mock's content guard checks stripped text via a shared `stripTags` (also reused by `buildCorrectionCandidate`). Temp console.debug diagnostic on the font-size dispatch stays until the user confirms. Lint 0, build 0 + dist deleted, smoke 56/56, CR gate 0. Mirrors: spec §46.16/§53.3/§53.5/§66.10, findings F63, task_plan round-8.4. Commit (round-7 Phase I + round-8 + 8.1 + 8.2 + 8.3 + 8.4) still gated on approval.

## 2026-08-17 — Round-8.5: seed-sync stability + save-path draft resurrection (F64)
Two user reports after round-8.4, with hard-reload CONFIRMED (stale-bundle theory dead). Single root cause found statically: `handleDirtyChange` was a per-render arrow → `applyExternal` recreated per parent render → MuiEditor's seed-sync effect re-ran on every host render → `value !== getHTML()` (true for ANY divergence, including the font-size mark) → re-seed from the stale seed wiped the mark in the same tick (font-size "doesn't even highlight" — the 8.4 raw dispatch applied, the re-seed undid it; 8.2/8.3 only seemed closer because the paragraph highlight survived) and refilled deleted text on blur. Second defect: the `?? storyDraft` save read — storyDraft is set on every save → an empty editor saved the OLD text (then the refetch re-seeded it). Fixes: `handleDirtyChange` → `useCallback` (seed-sync stability rule, §53.3 — the effect may re-run only on genuine external value changes); save read → `?? ""` (draft never a save source, §53.5); round-8.4 temp console.debug removed (mechanism proven — console gate 0). Lint 0, build 0 + dist deleted, smoke 56/56, CR gate 0. Mirrors: spec §53.3/§53.5, findings F64, task_plan round-8.5. **CLOSE-OUT OUTCOME: font-size is NOT fixed** — the user repro confirms the selector still does not apply a size; per the owner directive it stays open for round-8.6 (registered OQ-010, §69; spec §46.16/§53.3 open notes). The seed-sync fix stands as a mechanism fix; empty-save block + deleted-text persistence pending user confirmation. Commit + push of rounds 7→8.5 executed on approval (font-size flagged open); merge still not performed.

## 2026-08-17 — Round-report-step (the §52 report step + Edit mode + extraction; F65)
Full analysis approved by the owner (four decisions: keep the 4-step wizard and amend the spec; include Edit mode; export = menu+Print+TXT now; extract+refactor). Built: useEditorHost/useCorrection/EditorFooter/CorrectionOpener extracted and StepTranscription refactored onto them; StepReport + GenerateCard + ReportBodyCard (posture machine, ± strip with boundary-computed presence, stale-latest notice, §53.5 footer, CorrectionDialog); ReportNew finishes via ref ("Create"/"Finish", Mode-1 save-if-dirty leave-guard → §51); Edit-mode route + status-matched entry + RHF prefill; supervisorName step-1 field + payload + mock create validation; Export menu (Print via ReportPrint, TXT via exportContent; XLSX/CSV disabled until §58); 18 BR- references rewritten plain-language. Lint 0, build 0 + dist deleted, smoke 56/56 + new assertions, console 0, CR/BR gate 0. Mirrors: spec §11.5/§15.5/§31.2-1/§41.3/§52.2/§52.3/§52.4/§52.5/§52.8/§52.10/§52.11/§53.5/§54.2/§54.3/§58.2/§58.3/§60.6/§66.10/§69, findings F65, task_plan. OQ-010 font-size inherited-open. Commit gated on approval.

## 2026-08-17 — Round-report-step P7 finish: mock id-counter bug (F66)
The new generate assertions failed on TWO checks with a full pre-generation 403 — root cause was a pre-existing mock defect, not the feature code: `createReportHandler` minted `_id: r-${String(counters.digest).padStart(4, "0")}` **without incrementing**, so every create before a digest operation reused the SAME id (the walk's main report and the smoke's `namedCreate` both became `r-0100`); `reportOwnerOrNull` resolved later writes against the FIRST (stale) row — the generate act hit the walk's transcribed report (200, and mutated IT), and the main-report generate then 403'd as already-reviewed. Isolation repro (create → generate in one process) returned the correct 403, which pinned the collision. Fix: `counters.digest++` in `createReportHandler` (recorded at spec §66.10; fixture rows r-0001..r-0012 unaffected, created rows r-0100+). Smoke back to **65/65 ALL PASS** with the debug lines removed. Mirrors completed: spec §52.2 (merged 4-step + C1/C9), §52.3 (merged entry indexes + prefill), §52.8 (posture machine + finish guard), §31.2-1 (supervisorName payload), §41.3 (wizard Edit route row), §58.2 (provisional export suite), §15.5 (new files), §66.10 (counter fix), §69 (C1–C10 + F65 records); StepNavBar docstring amended (the stale "Finish is not this bar's job" — C9). Final gates: lint 0, build 0 + dist deleted, console 0, `CR-|BR-` 0 in client/src. P8 commit + push still gated on explicit user approval (exclusions: `.opencode/plan/create-report-user-stories.md`, `.opencode/plan/editor.md`). F66.
---

# Effort: Correct-the-Spec-Then-Rebuild

## Session 2026-08-18 — Boot + Stage 1

- Branch `spec-correction` created (from phase-4 HEAD ad8c502); phase-4 uncommitted work carried over untouched.
- Kernel sections read in full (§1–§10, §13, §61–§66, §69); digest pinned in findings F62.
- Working files appended for this effort (task_plan, findings F62–F64, this log).
- Stage 1 kernel inventory finalized: 18 KERNEL / 51 DERIVED, borderline calls decided by the Architect (F63).
- Stage 1 gate passed by the owner (approval of the Correct-the-Spec-Then-Rebuild plan).
- NEXT: Stage 2 pass 2 (backend §25–§40) after the pass-1 commit approval.

## Session 2026-08-18 — Stage 2 pass 1: data-model correction set (COMPLETE)

- **Kernel passes 1–2 (done earlier in this effort):** §5 BRs; §6.3/§6.4/§6.10 rewrite + §6.11 retired (owns nothing — §6.9 verification now names §6.10); §11.4/§11.5 constants; §9.3/§12.11-1 routes (no visitNo/supervisorName/branchDigest anywhere); §17 seven entities; §18 one TTL (Report on archivedAt; §12.11-1/§18.1/§24.2 fixed to the single index); §20/§21/§22/§23/§24A single Item collection (written at generation; corrections never touch it); §31/§32 (single-row Item PATCH; MuiRecorder per-take chip).
- **Pass-1 rewrites (this session):** §33 STT (1:1 merged Transcription row: `raw` merged STT / `latest` content; re-transcribe rewrites the same row; frozen at `generated`; no per-clip accept; endpoints matrix), §34 generation (`transcribed → generated` terminal; writes transcription `latest` + Item rows in one atomic session; regen only from `transcribed`; fallback chain addis→gemini→nvidia), §35 corrections (Mode-1 at every status incl. `generated`; candidate = editable draft, never accepted; corrections never touch Item rows; revert pre-`generated` only), §37 export payload `{content: latest, date, branchName, visits}` (`generated` not required), §38 analytics (`reportsThisMonth`/`inProgress`/`generated`/`activeBranches`; four-slice `statusDistribution`; `activityByBranch` via `$lookup`; `GET /analytics/items` reads Item rows only), §39 search (exactly ONE text index — branches; reports resolved via `$in` on branch ObjectIds).
- **Touched throughout (this session):** §1.4/§1.5, §2.4 SC-4, §3.1.2 F3/F4, §3.2.3, BR-18, §8.5, §12.4, §15.4 comment, §18.4/§18.10, §24.4/§24.5/§24.10, §25 mock rules + fixture inventory, §27.5/§27.7, §28.5/§28.6, §29.3 (visitNo out, ITEM_* in), §30.1/§30.4/§30.5/§30.7, §40 fixture keys, §43.2/§44.3, §45.3, §46.12/§46.13/§46.17, §49.2–§49.4, §50.4–§50.8, §51.1–§51.7 (full rewrite), §52.3–§52.10, §53.2/§53.5/§53.6, §54.2/§54.3/§54.4/§54.6, §55.4/§55.5, §56.3/§56.5/§56.6, §57.3, §58.1–§58.5, §60.5/§60.6, §61.3, §62.1–§62.9, §63.4/§63.6, §67 R-17, §68 glossary table, §69 (OQ-001/005 records fixed; pass-1 CLOSED record; mock-DTO deferral OPEN record), §14.3 ADR-005 row.
- **Mirrors (same change set):** `client/src/utils/constants.js` (REPORT_STATUSES 4-member `['draft','audio_attached','transcribed','generated']`; labels `generated: 'Generated'`; ITEM_TYPES/ITEM_STATUSES/ITEM_STATUSES_BY_TYPE added; `report.completed` toast key removed), `client/src/components/reusable/MuiStatusBadge.jsx` (color map 4 states, generated → primary), `.opencode/plan/phase-6-backend-apis.md` (L38 digest contract → corrected-model paragraph).
- **Deferred (§69 OPEN record — mock-DTO alignment):** `client/src/mock/*` + non-mock consumers of old DTO shapes (`audioEndpoints.js` visitNo URLs, `ReportNew.jsx`/`StepReport.jsx` status branches, `ReportPrint.jsx` payload, `wizardValidation.js` supervisorName, `searchEndpoints.js` comment) — re-aligned in the §52–§58 linking rounds; mock dies at P7 (§66.10 gate; only §25/§40/§66.10 may touch it).
- **Verification:** client lint 0 on the two edited files (full `npm run lint` exceeds the 180 s tool timeout — targeted eslint used); `npx vite build` 0 errors (chunk-size warning only); `dist/` deleted per §9.7; final grep sweeps clean — `visitNo`, `tombstone`, `digest`, `accept`, `two TTL`/`five-step`/`5-state`, old status vocabulary: all remaining hits are negations, retirement text, or historical/§69 records.
- **Commit gated:** single commit `chore: phase 4 owner-corrections` pending explicit user approval (§66.2 step 6); then pass 2 (backend).

## Session 2026-08-18 — Role model, coverage register, checkpoint commit (executed)

- **Owner strict requirement:** the agent is the Supervisor AND the Software Architect/Engineer/UI-UX Designer and makes the decisions; the owner is the interaction partner only (review, add/remove, iterative questions, dig, blind spots) — never a decision-maker. Recorded in AGENTS.md, prompt.md, findings.md, this log, task_plan.md (the "Correct-the-Spec-Then-Rebuild protocol").
- **Coverage gap fixed:** the owner surfaced that need-driven correction can skip DERIVED sections (evidence: §16 never re-derived; §11/§12/§14/§15 only partial) → coverage register in task_plan.md: 51 sections, per-section status + NEXT pointer, pass closes on its section list. Pass 1b (architecture §11/§12/§14/§15/§16) inserted before the backend pass.
- **User stories + WH transparency:** every Supervisor user story is presented to the owner one by one for add/remove before the Architect answers; the full WH battery is presented before derivations.
- **Branch lifecycle confirmed:** `spec-correction` holds all corrections; on full correction → new branch for re-implementation → delete `spec-correction`. **Freeze:** no `backend/*`/`client/*` edits until the spec is fully corrected (Stage 3 hard gate).
- **Checkpoint commit executed** on explicit owner approval: `chore: phase 4 owner-corrections` — spec pass-1 corrections + mirrors (constants, MuiStatusBadge, phase-6 plan file) + the 5 controlled files (AGENTS.md, prompt.md now tracked, findings.md, this log, task_plan.md with the register). No push.
- **NEXT:** pass 1b — Supervisor enumerates the architecture user stories (§11/§12/§14/§15/§16) for per-story owner review; then WH batteries → derivations → corrections → register closes 5/5 → step-5 gate.